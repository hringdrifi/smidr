---
title: はじめに
description: Smiðr の役割と、最初に知っておくと迷いにくい考え方。
---

<script setup>
import { withBase } from 'vitepress'
</script>

# はじめに

Smiðr は、VIA / Vial / ZMK Studio 互換キーボードのリマップと、カスタムキーボードの設計をブラウザ上で扱うためのツールです。手元のキーボードのキー割り当て変更から、KLE 互換のレイアウト編集、マトリクス、キーマップ、ハードウェア設定までを、同じ作業の流れで扱えることを目指しています。

自作キーボードの設計では、キーの物理配置、配線上の row / col、レイヤーごとのキー割り当て、ファームウェア向けの出力が別々の作業になりがちです。Smiðr では、それらをひとつのプロジェクトとして扱い、設計中の情報と実機で調整する情報をつなげて管理します。

## 3つのモード

Smiðr 0.5では、作業を **ハードウェア**、**ファームウェア**、**リマップ** の3モードに分けています。起動時のプロジェクトホームから新規作成、読込、実機接続を選べます。

手元のキーボードのキー割り当てを変えたい場合は、まず **リマップ** モードを使います。VIA / Vial または ZMK Studio 互換のキーボードに接続し、現在のキーマップを読み込んで編集します。

物理レイアウト、配線、ピン、基板出力には **ハードウェア** モードを使います。キーマップ、ライティング、Macro、Combo、Tap Dance、ファームウェア出力には **ファームウェア** モードを使います。

<figure>
  <img :src="withBase('/assets/smidr-remap-connected.png')" alt="接続済みキーボードを編集している Smiðr のリマップモード">
  <figcaption>一般的なキー割り当ての変更は、リマップモードで実機のキーマップを読み込んで編集します。</figcaption>
</figure>

## 基本の流れ

### リマップする

1. キーボードを PC に接続します。
2. Smiðr の **リマップ** モードを開きます。
3. `接続` から、キーボードが対応している接続方式を選びます。
4. キーボードからレイアウトとキーマップを読み込みます。
5. 変更したいキーを選び、キーパレットやキーマップ設定で新しいキーコードを割り当てます。
6. 必要に応じてバックアップを書き出します。

### 設計する

1. 新規プロジェクトを作成します。
2. プリセットで標準的なレイアウトを読み込むか、既存の KLE / VIA / Vial / QMK JSON を読み込みます。
3. ハードウェアの「レイアウト」でキーの位置、サイズ、回転を整えます。
4. 「配線」と「ピン」で各キーの行・列、MCU、ダイオード方向を設定します。
5. ファームウェアの「キーマップ」でレイヤーとキーコードを編集します。
6. 「振る舞い」でMacro、Combo、Tap Danceを設定します。
7. 編集後はヘッダーの保存ボタンで確定します。未保存の編集は復元用ドラフトとしてローカルに保持されます。「ビルドと出力」からQMK / VIA / Vial / ZMK等のファイルを書き出せます。
